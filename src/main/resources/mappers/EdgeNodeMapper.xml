<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.solvd.database.EdgeNodeMapper">

    <insert id="create" parameterType="org.solvd.model.EdgeNode" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO edges (from_id, to_id, weight, full_path, busses)
        VALUES (#{from}, #{to}, #{weight}, #{fullPath}, #{busses})
    </insert>

    <insert id="createList" parameterType="org.solvd.model.EdgeNode" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO edges (from_id, to_id, weight, full_path, busses)
        VALUES
        <foreach collection="list" item="edge" separator=",">
            (#{edge.from}, #{edge.to}, #{edge.weight}, #{edge.fullPath}, #{edge.busses})
        </foreach>
    </insert>

    <select id="read" parameterType="org.solvd.model.AddressNode" resultType="org.solvd.model.EdgeNode">
        SELECT * FROM edges WHERE from_id = #{from} AND to_id = #{to}
    </select>

    <update id="update" parameterType="org.solvd.model.EdgeNode">
        UPDATE edges
        SET from_id = #{from}, to_id = #{to}, weight = #{weight}, full_path = #{fullPath}
        WHERE from_id = #{from} AND to_id = #{to}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM edges
        WHERE from_id = #{from} AND to_id = #{to}
    </delete>

</mapper>
